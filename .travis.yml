os:
  - windows
  - linux
  - osx
language: rust
rust:
  - stable
  - beta
  - nightly
jobs:
  allow_failures:
    - rust: nightly
  fast_finish: true
install:
  - travis_wait 60 python tools/build.py build
  - cargo build -vv
script:
  - cargo run