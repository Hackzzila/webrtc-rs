os:
  - windows
  - linux
  - osx
language: rust
rust:
  - stable
  - beta
  - nightly
jobs:
  allow_failures:
    - rust: nightly
  fast_finish: true
env:
  - PYTHONHTTPSVERIFY=0
install:
  - python tools/build.py build
  - cargo build -vv
script:
  - cargo run